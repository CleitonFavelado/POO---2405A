fun main() {
    //POO - Programação Orientada a Objetios - Simular coisas da realidade na programação
    //
    //Objetos - possui Estados(nome, idade, endereço, altura e etc), comportamentos(andar, correr, dormir, comer etc)
    //a POO é dividida
    //
    //Classes - São modelos que servem para a construção de objs derivados dele mesmo(Ex Classe pessoa)
    //as classes recebem atriibutos(os Estados)
    //
    //Métodos - será as instancias da classe, as formas de agir
    //Estrutura de uma classe
    // class Nome(){ocorridos de um obj}
    /*
    val ricardo = Pessoa() //instaciondo um obj
    ricardo.altura = 2.15
    ricardo.corDosOlhos = "verde"
    ricardo.peso = 100.0
    ricardo.nome = "Ricardo  Grande de Oliweira"
    println(ricardo.altura) //para exibir um atributo em especifico
    
    val convidado1 = Pessoa("Claudio", 1.79, "castamho", 183.0)
    println(convidado1.peso)
    val convidado2 = Pessoa("Juliana", 1.5, "azul", 53.5)
    */
    val convidado1 = Pessoa("Chandão ", 1.83, "preto", 73.9)
    println(convidado1.nome)
    val convidado2 = Pessoa("Victor", 1.67, "castamho", 90.4)
    println(convidado2.nome)
    val convidado3 = Pessoa("Victoria", 1.59, "azul", 65.5)
    println(convidado3.nome)
    val convidado4 = Pessoa("Franco", 1.80, "castanho", 83.0)
    println(convidado4.nome)
    val convidado5 = Pessoa("Santiago", 1.76 , "preto", 48.0)
    println(convidado5.nome)
    val convidadoEspecial = Pessoa("Suchomimo",3.53, "castamho", 3000.0)
    /**


var dinossauro = Animal("Dinossauro",'M',"Rio Tietê","Carnívoro",397, true)
                        
   //println("Meu bichinho é um " + dinossauro.classe+" ele é " +dinossauro.dieta+" e ele mora no " +dinossauro.habitat+ "ele é muito legal!!! "+" todo mundo que passa por ele morre "+dinossauro.Atacar(true))
   dinossauro.nadando(false)       
                                                                
}
class Animal(val classe: String,val genero:Char,val habitat: String,
              val dieta: String, val idade: Int,  val voa:Boolean){
    
    /*fun Atacar(bravo: Boolean){
       
        if(bravo == true){
        println("Animal atacando")
        }else{
            println("Animal de boa, suavao")
            
    fun nadando(nadando: Boolean){
        if(nadando == true){
            println("Animal nadando")
        }else{
            println("se ele não está nadando ele está voando né" )
            */
                     val ContaCorrente = ContaNoBanco(1703, 13000.45, "Bradesco",
                                      "Guilherme Domingos Müller")
     ContaCorrente.consultarSaldo()
     //ContaCorrente.saque(13000.45)
     ContaCorrente.deposito(1000.00)
}

             
            
            class ContaNoBanco(var senha: Int, var saldo: Double,
                         var banco : String, var dono:String){
                         
          fun consultarSaldo(){
              println(" O saldo da sua conta é de R$13000.45")
              
              
          }
          /*fun saque(valorSaque: Double){
              if(valorSaque <= 0 ){
                  println("Impossível sacar R$$valorSaque")
              }else if(saldo >= valorSaque){
                  saldo -= valorSaque
                  println("Saque de R$$valorSaque realizado com sucesso")
                  
              }else{
                  println("Saldo insuficiente, saque de R$$valorSaque")
              }
          }
          */
           fun deposito(valorDeposito: Double){
              if(valorDeposito <= 0 ){
                  println("Impossível depositar R$$valorDeposito")
              }else{
                  saldo = saldo + valorDeposito
                  println("Deposito de R$$valorDeposito realizado com sucesso")
                  println("O seu saldo é de 14000.00 parabéns você é fino senhor")
              }
          }

        
    
    
        
    

}
    
    
        
    

//Métodos - Comportamentos da nossa classe, que os nossos receberão
//eles são declarados usando funções
    println(convidadoEspecial.altura.nome.peso)
}
class Pessoa(val nome: String,
             val altura:Double,
             val corDosOlhos: String,
             val peso: Double){
    //declarando atributos para a classe
    /*
    var nome = ""
    var altura = 0.0 
    var corDosOlhos = ""
    var peso = 0.0
    */
}
